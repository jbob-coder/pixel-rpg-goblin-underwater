name: Pixel RPG Migration Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Migration retired
        run: echo "Migration completed. pixel-rpg-goblin-underwater is now authoritative."
